# ImageZoomAnimationDemo
A Swift UIKit demo showcasing a fully interactive pinch-to-zoom image feed with smooth animations. Users can zoom images directly from a UITableView, and the zoomed image expands to full-screen size while maintaining aspect ratio, corner radius, and smooth transitions.
